# PostgreSQL connection
DB_HOST=localhost
DB_PORT=5432
DB_NAME=gametask
DB_USER=postgres
DB_PASSWORD=Chandu5432@m
DB_SYNC=true # set to true to auto-sync models on startup (dev only)
# If you prefer a full URL, percent-encode special characters in the password (e.g. @ -> %40).
# Example (do NOT commit real credentials):
# DATABASE_URL=postgresql://postgres:Chandu5432%40m@db.pwdcldxgciurmfhztdve.supabase.co:5432/postgres
# Alternative: set DB_HOST/DB_USER/DB_PASSWORD instead of a full DATABASE_URL.
DATABASE_URL=
# App
PORT=4000
